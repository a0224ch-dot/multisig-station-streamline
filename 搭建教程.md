# 分公司冷钱包多签站 · 精简搭建教程

给普通技术员用。按步骤做即可。  
正式域名示例：`multisig-station-branch.你的公司.com`  
本文以 **`multisig-station-branch.iqiyia.cyou`** 为例。

---

## 一、这是什么？

| 项目 | 说明 |
|------|------|
| 公网页 | 任何人打开就能扫码开通多签（免注册） |
| 后台 | 员工/加盟商登录后管理开通码、低档地址、广告等 |
| 总部 | 大额多签的钥匙和规则由总公司网站提供 |

**注意：** 分公司站和总公司站是两套网站，要**分开目录、分开域名、分开端口**，不要装在同一个站点里。

---

## 二、开始前准备

1. 一台已装好 **宝塔面板** 的 Linux 服务器（Ubuntu 22 常用）  
2. 域名已解析到这台服务器（A 记录）  
3. 知道总公司网址，例如：`https://multisig-station.iqiyia.cyou`  
4. 服务器上已有 **Node.js（18 或以上）** 和 **PM2**（宝塔「软件商店 / Node 版本管理」里可装）  
5. 能访问 GitHub 拉代码（私有仓库需登录或 Token）

---

## 三、一键安装（推荐）

打开宝塔 → **终端**，执行：

```bash
cd /www/wwwroot
git clone -b 20260815-1746 https://github.com/e12games/multisig-station-branch.git
cd multisig-station-branch
git pull origin 20260815-1746
bash deploy/install-branch-baota.sh
```

脚本会自动：

- 选一个**空闲端口**（一般避开 8787、8788，常用 **8790**）  
- 安装并启动后台服务（PM2 名称多为 `multisig-branch-api`）  
- 编译前端页面  

装完后，终端会打印类似：

```text
选用 API 端口: 8790
```

**把这个端口号记下来**，后面宝塔反代要用。

也可先只查端口：

```bash
bash deploy/check-ports.sh
```

---

## 四、在宝塔里建网站（必做）

### 1. 添加站点

- 域名：`multisig-station-branch.iqiyia.cyou`（换成你的）  
- **网站目录** 必须设为：

```text
/www/wwwroot/multisig-station-branch/frontend/dist
```

不要指到 `backend` 目录。

### 2. 申请 SSL

- 站点 → SSL → Let’s Encrypt（或已有证书）  
- 打开 **强制 HTTPS**

### 3. 反向代理（最容易错）

站点 → **反向代理** → 添加：

1. 打开 **高级功能**  
2. 按下面填：

| 项目 | 怎么填 |
|------|--------|
| 代理目录 | `/api`（小写） |
| 目标 URL | `http://127.0.0.1:8790`（端口以安装脚本为准） |
| 发送域名 | `$host` |

**千万注意：**

- 目标地址 **末尾不要加斜杠**（不要写成 `8790/`）  
- **不要**把「代理目录」设成 `/`（整站反代会坏掉）  
- 必须是小写 `/api`，不要写成 `/API`

点开该反代的 **「配置文件」**，确认类似：

```nginx
location ^~ /api/
{
    proxy_pass http://127.0.0.1:8790;
    proxy_set_header Host $host;
    ...
}
```

`8790` 后面**没有** `/`。

### 4. 解决后台登录 404（必做）

站点 → **配置文件**，找到 `location /`，改成：

```nginx
location / {
    try_files $uri $uri/ /index.html;
}
```

保存并 **重载 Nginx**。  
不做这一步，打开 `/branch/login` 会显示 nginx 的 404。

---

## 五、检查是否成功

浏览器打开：

| 地址 | 正常表现 |
|------|----------|
| `https://你的域名/` | 有二维码，显示当前网络（如 shasta） |
| `https://你的域名/branch/login` | 出现登录框 |
| `https://你的域名/api/health` | 显示类似 `{"ok":true,...}` |

默认后台账号：

- 账号：`admin`  
- 密码：`Branch@123456`  

**登录后请立刻改密码。**

总公司后台 → **分公司** 列表里，过一两分钟应能看到本站自动上报。

---

## 六、常见问题（对照处理）

| 现象 | 原因 | 处理 |
|------|------|------|
| 页面有字，但「当前网络 Not Found」、没有二维码 | `/api` 反代不对，路径被剥掉 | 检查反代：小写 `/api`，目标不要带末尾 `/` |
| `/branch/login` 显示 nginx 404 | 没配 SPA 回退 | `location /` 加上 `try_files ... /index.html` |
| 整站都是 JSON：`Route GET:/login not found` | 把整站 `/` 反代到 Node 了 | 删掉 `/` 反代，只保留 `/api` |
| 502 Bad Gateway | 端口错了或服务没开 | `pm2 list` 看是否 online；`curl http://127.0.0.1:端口/api/health` |
| 端口冲突 | 8787/8788 已被别的站占用 | 用安装脚本自动选口，或指定 `BRANCH_PORT=8791 bash deploy/install-branch-baota.sh` |

本机快速自检：

```bash
pm2 list
curl -s http://127.0.0.1:8790/api/health
curl -s https://你的域名/api/meta/public
```

（端口按实际为准。）

---

## 七、和总公司的关系（了解即可）

- 钱少：用分公司自己配的 **2 个地址** 做 2/3 多签  
- 钱多：自动向总公司要 **3 个地址** 做 3/4（页面上会提示「大额平台共管」）  
- 总公司可以后台 **关掉** 某家分公司拉取 3/4 的权限  
- **不要**在分公司改总公司的大额阈值；阈值以总公司为准  

---

## 八、端口怎么记（本机多网站时）

常见情况（仅作参考，以你服务器为准）：

| 端口 | 常见用途 |
|------|----------|
| 8787 | 可能已被别的系统占用 |
| 8788 | 常为总公司多签 API |
| 8790 | 常为分公司多签 API |

原则：**每个站用自己的端口，反代指对自己的端口。**

---

## 九、完成后检查清单

- [ ] 公网页能出二维码  
- [ ] `/branch/login` 能进后台  
- [ ] 已改默认密码  
- [ ] 总公司「分公司」列表能看到本站  
- [ ] 已配置分公司「多签地址」  
- [ ] 总公司高档 3 址已配齐（大额才用得上）  

---

## 十、以后怎么在线更新（装好后）

1. 用超级管理员登录后台  
2. 点左侧 **系统更新**  
3. **检查更新** → 有新版本再点 **立即更新**  
4. 等状态变成「成功」（失败会自动回滚）

说明：

- 更新包由总部发到 GitHub Release，清单在发布仓的 `latest.json`  
- 不会改你的 `.env` 和数据库  
- **第一次**若服务器上还没有「系统更新」菜单，需要按第三节重新拉代码/装一次；之后就可以在网页点更新  

总部如何打更新包：见项目里 `deploy/发版说明.md`。  
总部改功能前请先看 `CHANGELOG.md` 与 `docs/踩坑与决策.md`，避免重复踩坑。

---

## 十一、需要技术支持时请提供

1. 域名  
2. `pm2 list` 截图  
3. `curl -s http://127.0.0.1:你的端口/api/health` 结果  
4. 宝塔反代「配置文件」里 `location /api` 那一段  
5. 若更新失败：后台「系统更新」页的日志，或服务器目录 `.update-work/status.json`  

按本文做完，一般即可上线使用。
