# 精简多签站 · 搭建教程（白话版）

照着做就行，不用懂代码。  
举例域名：**`你的域名.com`**（换成你自己的）。

需要帮助：开发员电报 [@usdtsoft306](https://t.me/usdtsoft306)

**不想自己搭？** 可联系开发员代搭建（含服务器部署、宝塔站点、HTTPS、接口转发与验收），**收费 500 USDT**。

---

## 这站是干什么的？

- **客户用的页面**：打开 `https://你的域名.com/` → 扫二维码 → 在钱包里签名 → 开通多签  
- **你用的后台**：打开 `https://你的域名.com/branch/login` → 配地址、改网页、看已开通、系统更新  

装好后，浏览器访问 `https://你的域名.com/api/health`，能看到 `"edition":"streamline"` 就说明是精简版。

---

## 动手前要准备好

1. 一台 Linux 服务器，已安装 **宝塔面板**  
2. 域名已解析到这台服务器（A 记录指到服务器 IP）  
3. 宝塔里已安装 **Node.js 18+**、**PM2**（软件商店 → Node 版本管理）  
4. 服务器能访问 GitHub（拉代码用）

---

## 一共四步

| 步骤 | 做什么 |
|------|--------|
| ① | 在服务器终端跑安装脚本 |
| ② | 在宝塔里添加网站 + 开 HTTPS |
| ③ | 设置「接口转发」和「伪静态」（最容易漏） |
| ④ | 浏览器打开试试，进后台改密码、配多签地址 |

---

## ① 安装程序（宝塔 → 终端）

整段复制执行（**把域名改成你的**）：

```bash
cd /www/wwwroot
export SITE_DOMAIN='你的域名.com'
git clone -b 20260818-1044 https://github.com/a0224ch-dot/multisig-station-streamline.git
cd multisig-station-streamline
bash deploy/install-streamline-baota.sh
```

等它跑完。终端里会出现两行重要信息，**抄在纸上**：

- **API 端口**（常见是 `8791`）  
- **默认后台账号**：`admin` / `Branch@123456`（登录后马上改密码）

如果页面以后是空白的，再执行一次前端编译：

```bash
cd /www/wwwroot/multisig-station-streamline/frontend
npm run build
```

---

## ② 宝塔里建网站

1. **网站** → **添加站点** → 域名填 `你的域名.com`  
2. **网站目录**必须填：

   `/www/wwwroot/multisig-station-streamline/frontend/dist`

   （不要选 backend，也不要选上一级目录）

3. **SSL** → 申请证书 → 打开 **强制 HTTPS**

---

## ③ 接口转发 + 伪静态（必做）

### A. 接口转发（让登录、验证码能用）

1. 点进这个站点 → **反向代理** → **添加**  
2. 先打开 **高级功能**  
3. 按下面填：

| 填什么 | 怎么填 |
|--------|--------|
| 代理目录 | `/api` |
| 目标 URL | `http://127.0.0.1:8791`（改成你安装时记下的端口） |
| 发送域名 | `$host` |

**注意：目标 URL 末尾不要加 `/`**（不要写成 `8791/`），否则验证码会报 Not Found。

**只保留一份 `/api` 转发**，不要重复添加。

### B. 伪静态（让后台登录页能打开）

站点 → **伪静态**，粘贴一行：

```nginx
try_files $uri $uri/ /index.html;
```

保存，并重载 Nginx。

### 不会配？一键脚本（可选）

```bash
DOMAIN='你的域名.com' API_PORT=8791 \
  bash /www/wwwroot/multisig-station-streamline/deploy/fix-baota-nginx.sh
```

（`API_PORT` 改成你的实际端口。）

---

## ④ 打开试试

浏览器访问：

| 打开这个 | 应该看到 |
|----------|----------|
| `https://你的域名.com/` | 公网页 + 二维码 |
| `https://你的域名.com/branch/login` | 登录框，验证码能出图 |
| `https://你的域名.com/api/health` | 一行英文，里面有 `"edition":"streamline"` |

登录后台后，建议按顺序做：

1. **修改密码**（别再用默认密码）  
2. **网络设置** → 选主网或 Shasta 测试网  
3. **多签地址** → 填 2 个共管地址（和当前网络一致）  
4. **开通钱包** → 勾选客户常用的钱包  
5. **公网页装修** → 改标题、说明文字（可选）  

然后就可以把公网页链接或二维码给客户用了。

---

## 以后怎么更新？

登录后台 → **系统更新** → **检查更新** → **立即更新**。  
一般不用动服务器命令；更新不会删你的配置和数据库。

---

## 常见问题（一看就懂）

| 现象 | 怎么办 |
|------|--------|
| 后台登录页 404 | 检查伪静态有没有 `try_files … /index.html`；网站目录是不是 `frontend/dist` |
| 验证码显示 **Not Found** | 检查接口转发：端口要对、目标 URL **末尾不要 /** |
| 页面能开，登录/扫码都不行 | 多半没配 `/api` 转发，或转发错了整站 `/` |
| 公网页空白 | 重新 `npm run build`（见①） |
| 客户扫了码开通失败 | 后台先配好「多签地址」「开通钱包」；钱包里要有足够 TRX 付手续费 |
| TokenPocket / OKX 等 | 可用钱包首页扫码 |
| TronLink | **不要用首页扫一扫**；让客户在 TronLink 里点 **发现 / 浏览器**，粘贴开通链接 |

---

## 装完打勾

- [ ] 公网页能打开、有二维码  
- [ ] 后台能登录、验证码正常  
- [ ] 已改默认密码  
- [ ] 已选网络、配好多签地址  
- [ ] 已勾选开通钱包  

还有问题，电报联系 [@usdtsoft306](https://t.me/usdtsoft306)，说明你的域名和卡在哪一步即可。

**代搭建服务**：收费 **500 USDT**（含部署上线与基础验收；域名、服务器费用另计）。
