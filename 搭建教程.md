# 精简多签站 · VPS / 宝塔搭建教程

给普通技术员用。按步骤做即可。  
本文以域名 **`你的域名.com`**、安装目录 **`/www/wwwroot/multisig-station-streamline`** 为例。

| 项目 | 地址 |
|------|------|
| 源码仓 | https://github.com/e12games/multisig-station-streamline |
| 在线更新仓 | https://github.com/e12games/multisig-station-streamline-releases |
| 开发员电报 | https://t.me/usdtsoft306（`@usdtsoft306`） |

---

## 一、这是什么？

| 项目 | 说明 |
|------|------|
| 公网页 `/` | 客户扫码 / 打开链接即可开通多签（免注册） |
| 管理后台 `/branch/login` | 多签地址、已开通、公网页装修、开通钱包、系统更新、使用说明、修改密码 |

健康检查 `/api/health` 返回 `"edition":"streamline"`，用来确认装的是精简版。

> 本站与其它多签站（若同机）必须**分开目录、分开域名、分开 API 端口**。  
> 精简版默认优先端口 **8791**；**8788 不要占用**（常被其它站点使用）。

---

## 二、开始前准备

1. 一台已装好 **宝塔面板** 的 Linux 服务器（Ubuntu 22 常用）  
2. 域名已解析到这台服务器（A 记录）  
3. 服务器上已有 **Node.js 18+** 和 **PM2**（宝塔「软件商店 / Node 版本管理」里可装）  
4. 能访问 GitHub 拉代码  
5. 服务方提供的配置（若有）：`HQ_BASE_URL`、`BRANCH_API_KEY` 等，安装时可写进环境变量

---

## 三、一键安装（推荐）

打开宝塔 → **终端**，执行：

```bash
cd /www/wwwroot

# 换成你的域名
export SITE_DOMAIN='你的域名.com'

# 若服务方给了密钥/上游地址，可一并导出（没有则脚本会自动生成密钥）
# export HQ_BASE_URL='https://……'
# export BRANCH_API_KEY='……'

git clone -b 20260817-1952 https://github.com/e12games/multisig-station-streamline.git
cd multisig-station-streamline
bash deploy/install-streamline-baota.sh
```

脚本会自动：

- 选一个**空闲端口**（精简版优先 **8791** 起）  
- 写入 `backend/.env`、安装依赖、迁移数据库、写入初始管理员  
- 用 PM2 启动 API（进程名默认 `multisig-streamline-api`）  
- 编译前端到 `frontend/dist`

装完后，终端会打印类似：

```text
选用 API 端口: 8791
默认后台账号: admin / Branch@123456  （请立刻改密）
```

**把这个端口号记下来**，后面宝塔反代要用。

也可先只查端口：

```bash
bash deploy/check-ports.sh
```

指定端口安装示例：

```bash
SITE_DOMAIN='你的域名.com' SITE_PORT=8791 bash deploy/install-streamline-baota.sh
```

### 安装后立刻检查前端产物

网站目录必须能读到 `index.html`：

```bash
ls -lah /www/wwwroot/multisig-station-streamline/frontend/dist/index.html
```

若没有该文件，在服务器上重编一次：

```bash
cd /www/wwwroot/multisig-station-streamline/frontend
rm -rf dist
npm run build
ls -lah dist/index.html
```

---

## 四、在宝塔里建网站（必做）

### 1. 添加站点

- 域名：`你的域名.com`  
- **网站目录** 必须设为：

```text
/www/wwwroot/multisig-station-streamline/frontend/dist
```

不要指到 `backend`，也不要指到仓库根目录。

### 2. 申请 SSL

- 站点 → SSL → Let’s Encrypt（或已有证书）  
- 开启「强制 HTTPS」

### 3. 反代 API（只反代 `/api`，最容易错）

站点 → **反向代理** → 添加：

1. 先打开面板「**高级功能**」  
2. 按下面填：

| 项 | 值 |
|----|----|
| 代理目录 | `/api`（必须小写） |
| 目标 URL | `http://127.0.0.1:端口号`（安装脚本打印的端口，如 8791） |
| 发送域名 | `$host` |

**千万注意：**

- 目标地址 **末尾不要加斜杠**（不要写成 `8791/`）  
  写成 `8791/` 会把路径里的 `/api` 剥掉，后台验证码会显示红色 **Not Found**  
- **不要**把整站 `/` 反代到 Node；静态页由 nginx 直接读 `frontend/dist`  
- **只保留一份** `/api` 反代。宝塔建过一次后，不要再手工多写一份 `zz_multisig_api.conf`，否则 `nginx -t` 会报 `duplicate location "/api/"`  
- 多行粘贴时不要拆坏 `location /api/ {`，否则 `nginx -t` 整机失败  

点开该反代的 **「配置文件」**，确认类似：

```nginx
location ^~ /api/
{
    proxy_pass http://127.0.0.1:8791;
    proxy_set_header Host $host;
    ...
}
```

注意：`8791` 后面**没有** `/`。

### 4. SPA 伪静态（刷新子路由不 404）

站点 → 伪静态，或站点配置里的 `location /`，保证有：

```nginx
location / {
    try_files $uri $uri/ /index.html;
}
```

保存并 **重载 Nginx**。  
不做这一步，打开 `/branch/login` 常会显示 nginx 的 404。

### 5. 一键修 nginx（可选）

站点建好后，在终端执行（把域名和端口换成你的）：

```bash
DOMAIN='你的域名.com' API_PORT=8791 \
  bash /www/wwwroot/multisig-station-streamline/deploy/fix-baota-nginx.sh
```

脚本会备份配置、补 SPA、在需要时补 `/api` 反代；失败则自动回滚。  
若站点已有 `/api` 反代，脚本会**保持原配置**，不会再造一份重复 location。

---

## 五、验收

浏览器打开：

| 地址 | 正常表现 |
|------|----------|
| `https://你的域名.com/` | 公网页可开通 |
| `https://你的域名.com/branch/login` | 出现登录框，验证码可点出图形 |
| `https://你的域名.com/api/health` | JSON，且 `"edition":"streamline"` |

期望 `health` 类似：

```json
{"ok":true,"edition":"streamline","version":"……"}
```

默认管理员（安装种子）：

- 账号：`admin`  
- 密码：`Branch@123456`  

**登录后立刻改密**（后台「修改密码」）。

后台建议先完成：

1. 「多签地址」保存 2 个共管地址  
2. 「开通钱包」启用常用钱包  
3. 「公网页装修」按需要改文案 / 图  

公网页即可给客户扫码开通。

本机快速自检：

```bash
pm2 list
curl -s http://127.0.0.1:8791/api/health
curl -s http://127.0.0.1:8791/api/auth/captcha | head -c 120
curl -s https://你的域名.com/api/health
curl -s https://你的域名.com/api/auth/captcha | head -c 120
```

（端口按实际为准。域名侧应同样返回 JSON，而不是 HTML 404 页。）

---

## 六、常用运维命令

```bash
# 看进程
pm2 list
pm2 logs multisig-streamline-api --lines 80 --nostream

# 改完 .env 后重启
pm2 restart multisig-streamline-api --update-env

# 本机探活
curl -s http://127.0.0.1:8791/api/health
```

`.env` 位置：

```text
/www/wwwroot/multisig-station-streamline/backend/.env
```

重要项（按服务方要求填写即可）：

| 变量 | 含义 |
|------|------|
| `PORT` | API 端口，须与 nginx 反代一致 |
| `FRONTEND_ORIGIN` / `BRANCH_PUBLIC_URL` | 公网 HTTPS 域名 |
| `JWT_SECRET` | 登录令牌密钥，勿泄露 |
| `BRANCH_API_KEY` | 站点密钥（安装时可自动生成） |
| `HQ_BASE_URL` | 上游服务地址（按服务方提供） |
| `UPDATE_RELEASES_URL` | 在线更新清单地址 |
| `PM2_NAME` | 默认 `multisig-streamline-api` |
| `INSTALL_ROOT` | 默认 `/www/wwwroot/multisig-station-streamline` |

---

## 七、在线更新

后台「系统更新」→ 检查更新 → 立即更新。  

需在 `.env` 中配置（安装脚本默认已写）：

```env
UPDATE_RELEASES_URL=https://raw.githubusercontent.com/e12games/multisig-station-streamline-releases/main/latest.json
PM2_NAME=multisig-streamline-api
INSTALL_ROOT=/www/wwwroot/multisig-station-streamline
```

更新会保留 `.env` 与数据库；只跑迁移，不重跑 seed。

---

## 八、常见问题

| 现象 | 原因 | 处理 |
|------|------|------|
| `/branch/login` 显示 nginx 404 | 没配 SPA 回退，或网站目录不是 `frontend/dist` | `try_files $uri $uri/ /index.html;`；确认 `dist/index.html` 存在 |
| 登录页验证码红字 **Not Found** | `/api` 反代端口错，或 `proxy_pass` 末尾多了 `/` | 目标改成 `http://127.0.0.1:实际端口`（无末尾 `/`）；用第五节 curl 对比本机与域名 |
| 页面能开，接口全失败 / health 不是 JSON | `/api` 没反代，或反代了整站 `/` | 只保留 `/api` 反代；本机先 `curl http://127.0.0.1:端口/api/health` |
| `nginx -t` 报 `duplicate location "/api/"` | 同一站点有两份 `/api` | 删掉多余那份（常见为手工加的 `zz_multisig_api.conf`），只留宝塔反代或脚本生成的一份 |
| `nginx -t` 报奇怪的裸 `/api/` | 多行粘贴把 `location /api/ {` 拆坏 | 执行 `fix-baota-nginx.sh`，或注释裸 `/api/` 后再 `nginx -t && nginx -s reload` |
| 502 Bad Gateway | 端口错或 PM2 未起 | `pm2 list`；`curl http://127.0.0.1:端口/api/health` |
| `edition` 不是 `streamline` | 装错目录或反代指到别的多签站 | 核对网站目录、PM2 进程名、反代端口 |
| 空白页 / 没有 `index.html` | 前端未构建成功 | 见第三节「安装后立刻检查前端产物」 |
| 钱包开通失败 | 未配多签地址 / 未启钱包 / TRX 不足 | 先配「多签地址」「开通钱包」；客户需在钱包内置浏览器打开；地址上留足权限修改手续费 |

---

## 九、完成后检查清单

- [ ] `frontend/dist/index.html` 存在  
- [ ] 公网页 `/` 能打开  
- [ ] `/branch/login` 能进后台，验证码能出图  
- [ ] `/api/health` 返回 `"edition":"streamline"`  
- [ ] 已改默认密码  
- [ ] 已配置「多签地址」  
- [ ] 已启用「开通钱包」  

---

## 十、需要帮助

请尽量提供：

1. 域名  
2. `pm2 list` 截图  
3. `curl -s http://127.0.0.1:你的端口/api/health` 结果  
4. `curl -s https://你的域名/api/health` 结果  
5. 宝塔反代「配置文件」里 `location /api` 那一段  

开发员电报：[https://t.me/usdtsoft306](https://t.me/usdtsoft306)（`@usdtsoft306`）
