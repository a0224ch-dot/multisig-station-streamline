# 加密钱包精简多签站 · 搭建教程

> **中文为主，English below.** 更详细步骤见 [`搭建教程.zh-CN.md`](./搭建教程.zh-CN.md) / [`Setup-Guide.en.md`](./Setup-Guide.en.md)

---

## 中文 · 快速搭建（四步）

**环境：** Linux 服务器 + 宝塔面板，已装 Node.js 18+ 与 PM2，域名 A 记录已指向服务器。

### 1. 一键安装

```bash
cd /www/wwwroot
export SITE_DOMAIN='你的域名.com'
git clone -b main https://github.com/a0224ch-dot/multisig-station-streamline.git
cd multisig-station-streamline
bash deploy/install-streamline-baota.sh
```

记下脚本输出的 **API 端口**（常见 `8791`）。默认后台：`admin` / `Branch@123456`（**登录后立刻改密**）。

### 2. 宝塔建站点

- 网站目录：`/www/wwwroot/multisig-station-streamline/frontend/dist`
- 开启 HTTPS（建议强制 HTTPS）

### 3. 反代 + 伪静态

| 项 | 配置 |
|---|---|
| 反向代理 | 路径 `/api` → `http://127.0.0.1:8791`（端口以 `backend/.env` 为准） |
| 伪静态 | `try_files $uri $uri/ /index.html;` |

### 4. 验证

打开 `https://你的域名.com/api/health`，应含 `"edition":"streamline"`。

| 页面 | 地址 |
|---|---|
| 介绍首页 | `/` |
| 后台登录 | `/login` |
| 站点开通 | `/open` |
| 会员出码 | `/p/u/{会员码}` |

进后台：改密码 → 选网络 → 配 2 个多签地址 → 可选开会员注册 / 场景二维码。

**在线更新：** 后台 → 系统更新 → 检查更新 → 立即更新（保留 `.env` 与数据库）。

---

## English · Quick setup (4 steps)

**Prerequisites:** Linux + Baota panel, Node.js 18+, PM2, domain DNS ready.

### 1. Install

```bash
cd /www/wwwroot
export SITE_DOMAIN='your-domain.com'
git clone -b main https://github.com/a0224ch-dot/multisig-station-streamline.git
cd multisig-station-streamline
bash deploy/install-streamline-baota.sh
```

Note the **API port** (often `8791`). Default admin: `admin` / `Branch@123456` — **change password immediately**.

### 2. Baota site

- Site root: `/www/wwwroot/multisig-station-streamline/frontend/dist`
- Enable HTTPS

### 3. Proxy + SPA rewrite

| Item | Value |
|---|---|
| Reverse proxy | `/api` → `http://127.0.0.1:8791` (match `PORT` in `backend/.env`) |
| Rewrite | `try_files $uri $uri/ /index.html;` |

### 4. Verify

`https://your-domain.com/api/health` must return `"edition":"streamline"`.

Then: change password → set network → configure 2 multisig addresses → optional member/scenario setup.

**OTA:** Admin → System Update → Check → Update now (keeps `.env` and DB).
